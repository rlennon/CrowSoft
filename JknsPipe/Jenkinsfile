pipeline {
  agent any
  stages {
    stage('Checkout code') {
	  	steps {
		  	checkout scm				
				}
			}	
	  }
  	stage('Selenium Testing') {
  		steps {
			
 		  }	
	  }
				stage('Build/Zip Artifacts') {
	  	steps {
		  
			
		  }	
	  }
  	stage('Artifactory Upload') {
  		steps {
		}	
	  }
 }

