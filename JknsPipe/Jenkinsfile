pipeline {
  agent any
  stages {
    stage('Checkout code') {
	  	steps {
		  	checkout scm				
				}
			}	
    stage('Run NUnit Test') {
	  	steps {
		  		echo 'Nunit test will go in here'			
				}
			}	
		
    stage('Upload to Artifactory') {
	  	steps {
		  		echo 'Artifactory will go in here'			
				}
			}	
	 stage('Stopapp') {
	  	steps {
		  	sh label: '', script: 'echo Cr0wadm!n | sudo -S systemctl stop crowsoft-webapp.service'				
				}
			}	
	 
	stage('Startapp') {
	  	steps {
		  	sh label: '', script: 'echo Cr0wadm!n | sudo -S systemctl start crowsoft-webapp.service'				
				}
			}	
	  }


 }

